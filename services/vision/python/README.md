# Python vision prototype

`ai01.analyze` is a pure BGR-array to deterministic evidence function, isolated from
camera access and storage. The host adapter, scene contract, tests, dependencies,
and C++ compatibility limits are documented in [tools/ai01](../../../tools/ai01/README.md).
