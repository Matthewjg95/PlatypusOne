# third_party

Vendored external dependencies. Currently empty by design — the core builds
dependency-free. Planned additions (see docs/ROADMAP.md): Catch2 (tests),
libcamera bindings (camera backend). Each vendored library gets its own
subdirectory with LICENSE and a VERSION pin.
