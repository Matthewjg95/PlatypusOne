# apps/shadowscan

Planned application. Implement platypus::appfw::IApp following apps/settings as the reference pattern, then register in the composition root (apps/launcher/src/main.cpp). See docs/ROADMAP.md.
